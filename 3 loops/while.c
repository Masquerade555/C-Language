#include <stdio.h>

main()
{
    int a;
    a = 1;

    // using while loop
    // if a is less then or equal to 10 then stop this loop
    while (a <= 10)
    {

        // this lise will print until above condition is false
        printf("%d \t", a);

        // a will increase by 1
        a++;
    }
}